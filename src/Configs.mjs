
export const defaultListenerConfig = {
    priority: 1
}

/**
 * @type {EventConfig}
 */
export const defaultEventConfig = {
    exact: false
}

export const defaultBusConfig = {
    debug: false,
    suppress: true,
    async: false
}